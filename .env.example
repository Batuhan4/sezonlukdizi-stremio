# SezonlukDizi Stremio Addon Configuration
# Copy this file to .env and update values

# Server port (default: 7000)
PORT=7000

# Public URL of your addon server (required for TV playback)
BASE_URL=http://localhost:7000

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Main site domain (rotates periodically; default: sezonlukdizi.cc)
SITE_DOMAIN=sezonlukdizi.cc

# Proxy mode: never, auto, always (default: never)
# never = never use proxy (default — direct access works from Turkey)
# auto/always = use Turkey-located public proxies (deployments outside Turkey,
#               where Cloudflare may geo-restrict the site)
PROXY_ENABLED=never
